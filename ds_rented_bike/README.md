**Вид и тип задачи**

Это задача регрессии: нужно предсказывать численное значение спроса — Rented Bike Count (количество аренд за час).

Тип задачи — обучение с учителем: у нас есть исторические данные с признаками (погода, календарь, время суток) и известным значением целевой переменной для каждого примера.


Стек:

Вывод:

* Сравнены модели для прогнозирования количества арендованных велосипедов.
* Лучшая модель — **kNN** с параметрами `n_neighbors=8`, `weights='distance'`, `p=1`.
* На тестовой выборке модель показала:

  * **RMSE — 309.81**
  * **MAE — 205.78**
  * **R² — 0.765**
* Наибольшее влияние на спрос оказывают **погодные условия, температура, влажность, сезон и время суток**.
* Модель хорошо обобщается на новые данные и может использоваться для **планирования распределения велосипедов и технического обслуживания**.

**Итог:** kNN показала стабильное качество прогнозирования спроса и может применяться для оптимизации работы сервиса проката.
